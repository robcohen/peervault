\* TLC Configuration for sync_protocol_v3.tla
\* Run: tlc -config sync_protocol_v3.cfg sync_protocol_v3.tla

SPECIFICATION Spec

\* Model values
CONSTANTS
    Peers = {A, B}
    Files = {f1}
    Blobs = {b1}
    MaxVersion = 3
    NULL = NULL

\* Invariants to check
INVARIANT NoReadErrors
INVARIANT KeyExchangeCorrect
INVARIANT ValidSessionTransitions
INVARIANT LiveModeRequiresKeyAgreement
