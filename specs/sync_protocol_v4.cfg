\* TLC Configuration for sync_protocol_v4.tla
\* Run: tlc -config sync_protocol_v4.cfg sync_protocol_v4.tla

SPECIFICATION Spec

\* Model values
CONSTANTS
    Peers = {A, B}
    VaultIds = {V1}
    MaxVersion = 3
    NULL = NULL

\* Invariants to check
INVARIANT LiveModeKeyAgreement
INVARIANT LiveModeSameVault
INVARIANT KeyExchangeCorrect
INVARIANT ValidSessionStates
INVARIANT VersionInfoAfterConnect
INVARIANT NoKeyConflictInLiveMode

\* This should reveal the key conflict deadlock
INVARIANT KeyMismatchDeadlock
