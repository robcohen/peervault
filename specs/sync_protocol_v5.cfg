\* TLC Configuration for sync_protocol_v5.tla
\* Run: tlc -config sync_protocol_v5.cfg sync_protocol_v5.tla

SPECIFICATION Spec

CONSTANTS
    Peers = {A, B}
    VaultIds = {v1}
    BlobHashes = {b1}
    Docs = {d1}
    MaxVersion = 1
    NULL = NULL

INVARIANT ReferentialIntegrity
\* PROPERTY EventualKeyResolution
