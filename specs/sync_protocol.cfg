\* TLC Configuration for sync_protocol.tla
\* Run: tlc sync_protocol.tla -config sync_protocol.cfg

SPECIFICATION Spec

\* Model values
CONSTANTS
    Peers = {A, B}
    Files = {f1}
    MaxVersion = 3
    NULL = NULL

\* Invariants to check (these WILL fail, exposing bugs)
INVARIANT NoFormatErrors
INVARIANT TicketsHaveAddressing
INVARIANT ContentReadable

\* Properties
PROPERTY EventualConvergence
