\* TLC Configuration for sync_protocol_v2.tla (corrected protocol)
\* Run: tlc sync_protocol_v2.tla -config sync_protocol_v2.cfg

SPECIFICATION Spec

\* Model values
CONSTANTS
    Peers = {A, B}
    Files = {f1}
    MaxVersion = 3
    NULL = NULL

\* Safety Invariants (should all PASS)
INVARIANT NoFormatErrors
INVARIANT TicketsHaveAddressing
INVARIANT ContentReadable
INVARIANT SyncRequiresAgreement

\* Note: ContentConvergence is a liveness property (eventual), not safety
\* PROPERTY EventualConvergence
